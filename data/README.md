This directory contains several subfolders:

* ````raw````: data without modifications.
* ````processed````: data ready to be manipulared
* ````formats````: json files to match each of the <i>pagadoras</i> to the general db structure. 